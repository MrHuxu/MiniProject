<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Question Collector" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
      <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Question Collector</a>
        <a class="navbar-brand" href="/"><%= image_tag 'logo.png', alt: 'Brand', id: 'logo' %></a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <div class="navbar-right">
            <% if author_signed_in? %>
              Signed in as <%= link_to current_author.email, '/authors/edit' %>
            <%= link_to "Sign out", destroy_author_session_path, :method => :delete, class: "btn btn-danger navbar-btn" %>
          <% else %>
            <%= link_to "Sign up", new_author_registration_path, class: 'btn btn-success navbar-btn' %> or <%= link_to "Sign in", new_author_session_path, class: 'btn btn-default navbar-btn' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="container">
        <%= bootstrap_flash %>
        <%= yield %>
  </div> <!-- /container -->

</body>
</html>
